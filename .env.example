# Sanity — copy this file to `.env` and fill in the values.
# Find these in the Sanity dashboard (sanity.io/manage) → your project.
# Neither value is a secret: the dataset is public, so reads need no token.

# Project → API → Project ID
PUBLIC_SANITY_PROJECT_ID=

# Usually "production".
PUBLIC_SANITY_DATASET=production
